// Stub — the install/onboarding page is not ported this phase. The existing
// Rust-rendered /install page keeps serving the real flow in parallel.
export function InstallPage() {
  return (
    <section class="settings-card">
      <h2>Install</h2>
      <p>Not ported yet. The existing install page still works at the backend.</p>
      <p>
        <a href="/install">Open the current install page →</a>
      </p>
    </section>
  );
}
